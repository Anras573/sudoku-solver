# sudoku-solver

A TypeScript + React Sudoku board boilerplate.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## GitHub Pages Deployment

The app is automatically built and deployed to GitHub Pages via `.github/workflows/deploy.yml` on pushes to `main`.
